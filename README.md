# Список задач для вивчення масивів, циклів та об'єктів в JavaScript

## Задача 1: Перевірка парності чисел

**Опис**: Створіть функцію, яка приймає масив чисел і повертає новий масив, що містить тільки парні числа.

**Що приймає**: Масив чисел  
**Що повертає**: Новий масив з парними числами

**Приклади**:

- `filterEvenNumbers([1, 2, 3, 4, 5, 6])` → `[2, 4, 6]`
- `filterEvenNumbers([1, 3, 5])` → `[]`
- `filterEvenNumbers([2, 8, 10])` → `[2, 8, 10]`

---

## Задача 2: Підрахунок символів у рядку

**Опис**: Створіть функцію, яка приймає рядок і повертає кількість кожного символу у вигляді об'єкта.

**Що приймає**: Рядок  
**Що повертає**: Об'єкт з підрахунком символів

**Приклади**:

- `countCharacters("hello")` → `{h: 1, e: 1, l: 2, o: 1}`
- `countCharacters("aaa")` → `{a: 3}`
- `countCharacters("abc")` → `{a: 1, b: 1, c: 1}`

---

## Задача 3: Видалення дублікатів з масиву

**Опис**: Створіть функцію, яка приймає масив і повертає новий масив без повторюваних елементів.

**Що приймає**: Масив будь-яких елементів  
**Що повертає**: Новий масив без дублікатів

**Приклади**:

- `removeDuplicates([1, 2, 2, 3, 4, 4, 5])` → `[1, 2, 3, 4, 5]`
- `removeDuplicates(["a", "b", "a", "c"])` → `["a", "b", "c"]`
- `removeDuplicates([1, 1, 1])` → `[1]`

---

## Задача 4: Сума цифр числа

**Опис**: Створіть функцію, яка приймає число і повертає суму всіх його цифр.

**Що приймає**: Число  
**Що повертає**: Сума цифр числа

**Приклади**:

- `sumDigits(123)` → `6` (1 + 2 + 3)
- `sumDigits(999)` → `27` (9 + 9 + 9)
- `sumDigits(0)` → `0`

---

## Задача 5: Робота з масивом об'єктів (студенти)

**Опис**: Створіть функцію, яка приймає масив об'єктів студентів (з властивостями name та grade) і повертає масив імен студентів, які мають оцінку вище за 75.

**Що приймає**: Масив об'єктів студентів  
**Що повертає**: Масив імен успішних студентів

**Приклади**:

- `getTopStudents([{name: "Anna", grade: 85}, {name: "Bob", grade: 65}])` → `["Anna"]`
- `getTopStudents([{name: "John", grade: 90}, {name: "Jane", grade: 80}])` → `["John", "Jane"]`

---

## Задача 6: Перевірка зростаючої послідовності

**Опис**: Створіть функцію, яка перевіряє, чи масив чисел відсортований у зростаючому порядку.

**Що приймає**: Масив чисел  
**Що повертає**: Boolean (true/false)

**Приклади**:

- `isAscending([1, 2, 3, 4, 5])` → `true`
- `isAscending([1, 3, 2, 4])` → `false`
- `isAscending([5, 5, 5])` → `false`

---

## Задача 7: Групування слів за довжиною

**Опис**: Створіть функцію, яка приймає масив слів і повертає об'єкт, де ключі - це довжина слів, а значення - масиви слів відповідної довжини.

**Що приймає**: Масив рядків  
**Що повертає**: Об'єкт з групуванням за довжиною

**Приклади**:

- `groupByLength(["cat", "dog", "bird", "fish"])` → `{3: ["cat", "dog"], 4: ["bird", "fish"]}`
- `groupByLength(["a", "bb", "ccc"])` → `{1: ["a"], 2: ["bb"], 3: ["ccc"]}`

---

## Задача 8: Пошук другого найбільшого числа

**Опис**: Створіть функцію, яка знаходить друге за величиною число в масиві.

**Що приймає**: Масив чисел  
**Що повертає**: Друге найбільше число або null, якщо його немає

**Приклади**:

- `secondLargest([1, 3, 4, 5, 2])` → `4`
- `secondLargest([10, 10, 10])` → `null`
- `secondLargest([5, 1])` → `1`

---

## Задача 9: Перетворення об'єкта в масив

**Опис**: Створіть функцію, яка приймає об'єкт і повертає масив масивів, де кожен внутрішній масив містить ключ і значення.

**Що приймає**: Об'єкт  
**Що повертає**: Масив пар [ключ, значення]

**Приклади**:

- `objectToArray({a: 1, b: 2, c: 3})` → `[["a", 1], ["b", 2], ["c", 3]]`
- `objectToArray({name: "John", age: 25})` → `[["name", "John"], ["age", 25]]`

---

## Задача 10: Фільтрація за віком та статтю

**Опис**: Створіть функцію, яка приймає масив об'єктів людей (з властивостями name, age, gender) і повертає масив імен жінок старше 30 років.

**Що приймає**: Масив об'єктів людей  
**Що повертає**: Масив імен жінок старше 30

**Приклади**:

- `filterWomenOver30([{name: "Anna", age: 35, gender: "female"}, {name: "Bob", age: 40, gender: "male"}])` → `["Anna"]`

---

## Задача 11: Обертання масиву

**Опис**: Створіть функцію, яка "обертає" масив на задану кількість позицій вправо.

**Що приймає**: Масив та число позицій для обертання  
**Що повертає**: Новий обернутий масив

**Приклади**:

- `rotateArray([1, 2, 3, 4, 5], 2)` → `[4, 5, 1, 2, 3]`
- `rotateArray(["a", "b", "c"], 1)` → `["c", "a", "b"]`

---

## Задача 12: Пошук найдовшого слова

**Опис**: Створіть функцію, яка приймає масив слів і повертає найдовше слово. Якщо є кілька слів однакової довжини, поверніть перше з них.

**Що приймає**: Масив рядків  
**Що повертає**: Найдовший рядок

**Приклади**:

- `longestWord(["cat", "elephant", "dog"])` → `"elephant"`
- `longestWord(["hello", "world"])` → `"hello"`

---

## Задача 13: Сортування об'єктів за властивістю

**Опис**: Створіть функцію, яка приймає масив об'єктів товарів (з властивостями name та price) і повертає масив, відсортований за ціною від найменшої до найбільшої.

**Що приймає**: Масив об'єктів товарів  
**Що повертає**: Відсортований масив об'єктів

**Приклади**:

- `sortByPrice([{name: "apple", price: 1.5}, {name: "banana", price: 0.8}])` → `[{name: "banana", price: 0.8}, {name: "apple", price: 1.5}]`

---

## Задача 14: Злиття відсортованих масивів

**Опис**: Створіть функцію, яка приймає два відсортованих масиви чисел і повертає один відсортований масив, що містить всі елементи з обох масивів.

**Що приймає**: Два відсортованих масиви чисел  
**Що повертає**: Один відсортований масив

**Приклади**:

- `mergeSortedArrays([1, 3, 5], [2, 4, 6])` → `[1, 2, 3, 4, 5, 6]`
- `mergeSortedArrays([1, 5], [2, 3, 4])` → `[1, 2, 3, 4, 5]`

---

## Задача 15: Аналіз продажів по місяцях

**Опис**: Створіть функцію, яка приймає масив об'єктів продажів (з властивостями month, amount) і повертає об'єкт з найуспішнішим місяцем (найбільша сума) та загальною сумою продажів.

**Що приймає**: Масив об'єктів продажів  
**Що повертає**: Об'єкт з інформацією про результати

**Приклади**:

- `analyzeSales([{month: "Jan", amount: 1000}, {month: "Feb", amount: 1500}])` → `{bestMonth: "Feb", totalSales: 2500}`
- `analyzeSales([{month: "Mar", amount: 800}, {month: "Apr", amount: 800}])` → `{bestMonth: "Mar", totalSales: 1600}`

---

# 📚 Шпаргалка: Корисні методи для виконання завдань

## 🔄 Методи масивів

### Додавання/видалення елементів:

- `arr.push(element)` - додає елемент в кінець масиву
- `arr.pop()` - видаляє останній елемент
- `arr.unshift(element)` - додає елемент на початок
- `arr.shift()` - видаляє перший елемент
- `arr.splice(index, deleteCount, ...items)` - видаляє/додає елементи

### Пошук і перевірка:

- `arr.indexOf(element)` - знаходить індекс елемента (-1 якщо не знайдено)
- `arr.includes(element)` - перевіряє чи є елемент в масиві (true/false)
- `arr.find(callback)` - знаходить перший елемент за умовою
- `arr.findIndex(callback)` - знаходить індекс першого елемента за умовою

### Перетворення масивів:

- `arr.map(callback)` - створює новий масив з перетвореними елементами
- `arr.filter(callback)` - створює новий масив з елементами, що пройшли фільтр
- `arr.reduce(callback, initialValue)` - зводить масив до одного значення
- `arr.sort(compareFunction)` - сортує масив
- `arr.reverse()` - обертає масив
- `arr.slice(start, end)` - створює копію частини масиву
- `arr.concat(array2)` - об'єднує масиви

### Перевірка всіх/деяких елементів:

- `arr.every(callback)` - перевіряє чи всі елементи відповідають умові
- `arr.some(callback)` - перевіряє чи хоча б один елемент відповідає умові

### Ітерація:

- `arr.forEach(callback)` - виконує функцію для кожного елемента
- `for...of` - цикл для ітерації по елементах
- `for (let i = 0; i < arr.length; i++)` - звичайний цикл з індексом

## 📝 Методи рядків

### Перетворення:

- `str.toLowerCase()` - переводить у нижній регістр
- `str.toUpperCase()` - переводить у верхній регістр
- `str.split(separator)` - розбиває рядок на масив
- `str.slice(start, end)` - вирізає частину рядка
- `str.trim()` - видаляє пробіли з початку і кінця

### Пошук:

- `str.indexOf(substring)` - знаходить позицію підрядка
- `str.includes(substring)` - перевіряє чи містить підрядок
- `str.charAt(index)` - повертає символ за індексом

### Властивості:

- `str.length` - довжина рядка

## 🔑 Робота з об'єктами

### Отримання ключів/значень:

- `Object.keys(obj)` - повертає масив ключів
- `Object.values(obj)` - повертає масив значень
- `Object.entries(obj)` - повертає масив пар [ключ, значення]

### Перевірка властивостей:

- `obj.hasOwnProperty(key)` - перевіряє чи є властивість в об'єкті
- `key in obj` - альтернативний спосіб перевірки

### Ітерація по об'єкту:

- `for (let key in obj)` - цикл по ключах об'єкта
- `for (let [key, value] of Object.entries(obj))` - цикл по парах ключ-значення

## 🔢 Математичні операції

### Основні:

- `Math.max(...array)` - максимальне значення
- `Math.min(...array)` - мінімальне значення
- `Math.abs(num)` - абсолютне значення
- `Math.floor(num)` - округлення вниз
- `Math.ceil(num)` - округлення вгору
- `Math.round(num)` - звичайне округлення

### Перевірки:

- `num % 2 === 0` - перевірка на парність
- `isNaN(value)` - перевірка чи не є число

## 🛠️ Корисні техніки

### Видалення дублікатів:

```javascript
// Використання Set
[...new Set(array)];

// Використання filter + indexOf
array.filter((item, index) => array.indexOf(item) === index);
```

### Перетворення числа в масив цифр:

```javascript
// Через рядок
String(number).split('').map(Number);

// Математично
while (num > 0) {
  digits.push(num % 10);
  num = Math.floor(num / 10);
}
```

### Сортування об'єктів:

```javascript
// За числовою властивістю
array.sort((a, b) => a.property - b.property);

// За рядковою властивістю
array.sort((a, b) => a.property.localeCompare(b.property));
```

### Групування елементів:

```javascript
// Базовий шаблон
const result = {};
for (let item of array) {
    const key = /* логіка для ключа */;
    if (!result[key]) {
        result[key] = [];
    }
    result[key].push(item);
}
```

### Пошук елементів за умовою:

```javascript
// Фільтрація за кількома умовами
array.filter((item) => condition1 && condition2);

// Пошук максимального за певною властивістю
array.reduce((max, current) =>
  current.property > max.property ? current : max,
);
```
